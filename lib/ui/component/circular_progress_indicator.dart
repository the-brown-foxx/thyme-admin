import 'package:flutter/material.dart';

class HerbHubCircularProgressIndicator extends StatelessWidget {
  const HerbHubCircularProgressIndicator({super.key});

  @override
  Widget build(final BuildContext context) {
    return const CircularProgressIndicator(backgroundColor: Colors.transparent,);
  }
}
